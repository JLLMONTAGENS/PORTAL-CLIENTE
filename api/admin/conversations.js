const {getDatabase}=require('../_db');const {currentUser}=require('./_auth');
module.exports=async function handler(req,res){if(req.method!=='GET')return res.status(405).json({error:'Método não permitido'});if(!currentUser(req))return res.status(401).json({error:'Sessão expirada.'});try{const status=String(req.query?.status||'').trim();const sql=getDatabase();const rows=status?await sql`SELECT id,numero_atendimento,nome_cliente,telefone,status,detalhes_servico,agendado_para,valor_servico_centavos,criado_em,atualizado_em FROM atendimentos WHERE status=${status} ORDER BY atualizado_em DESC LIMIT 100`:await sql`SELECT id,numero_atendimento,nome_cliente,telefone,status,detalhes_servico,agendado_para,valor_servico_centavos,criado_em,atualizado_em FROM atendimentos ORDER BY atualizado_em DESC LIMIT 100`;return res.status(200).json({atendimentos:rows})}catch(error){console.error(error);return res.status(500).json({error:'Não foi possível carregar os atendimentos.'})}};
